<Window x:Class="EQAPO_Configurator.Controls.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EQAPO_Configurator.Controls"
        Title="EQAPO Overlay"
        Width="320" Height="220"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="0" Top="0"
        Opacity="0"
        SourceInitialized="OnSourceInitialized"
        ContentRendered="OnContentRendered">

    <Grid Margin="8">
        <Border x:Name="OverlayBorder"
                Background="#D9101010"
                CornerRadius="8"
                Padding="10,8"
                BorderBrush="#33FFFFFF"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Row 0: Status + Profile name + Genre -->
                <StackPanel Grid.Row="0" Margin="0,0,0,4">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                        <Ellipse Width="6" Height="6"
                                 Fill="#00CC66"
                                 Margin="0,0,6,0"
                                 VerticalAlignment="Center" />
                        <TextBlock x:Name="StatusText"
                                   Text="EQ ACTIVE"
                                   FontFamily="Segoe UI"
                                   FontSize="10"
                                   FontWeight="Bold"
                                   Foreground="#00CC66"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock x:Name="ProfileNameText"
                               Text="No Profile"
                               FontFamily="Segoe UI"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#FFFFFF"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="InfoText"
                               Text=""
                               FontFamily="Segoe UI"
                               FontSize="9"
                               Foreground="#888888"
                               TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <!-- Row 1: Spectrum (hidden by default, toggled via settings) -->
                <local:CompactSpectrum x:Name="Spectrum"
                                       Grid.Row="1"
                                       Margin="0,2,0,0"
                                       Height="20"
                                       Visibility="Collapsed" />

                <!-- Row 2: Per-category volume levels (fills remaining space) -->
                <local:BandLevelsBar x:Name="BandLevels" Grid.Row="2" Margin="0,4,0,0" />
            </Grid>
        </Border>
    </Grid>
</Window>
