<ui:FluentWindow x:Class="EQAPO_Configurator.HeadphoneSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Headphone Setup"
        Width="700" Height="600"
        MinWidth="500" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <ui:TitleBar Grid.Row="0" Title="Headphone Setup" ShowMaximize="False" />

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,8">
            <StackPanel MaxWidth="600" HorizontalAlignment="Center">

                <!-- Device Detection -->
                <TextBlock Text="Detected Audio Device"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12" />

                <ui:CardControl Margin="0,0,0,8" Padding="12">
                    <ui:CardControl.Header>
                        <StackPanel>
                            <TextBlock Text="Device"
                                       FontSize="12"
                                       FontWeight="SemiBold" />
                            <TextBlock x:Name="DetectedDeviceText"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </ui:CardControl.Header>
                </ui:CardControl>

                <ui:CardControl Margin="0,0,0,16" Padding="12">
                    <ui:CardControl.Header>
                        <StackPanel>
                            <TextBlock Text="Audio Mode"
                                       FontSize="12"
                                       FontWeight="SemiBold" />
                            <TextBlock x:Name="DetectedModeText"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </ui:CardControl.Header>
                </ui:CardControl>

                <!-- Divider -->
                <Separator Margin="0,0,0,16"
                           Background="{DynamicResource CardStrokeColorDefaultBrush}" />

                <!-- Headphone Search -->
                <TextBlock Text="Download Headphone EQ Profile"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4" />

                <TextBlock Text="Search for your headphone model to download a correction profile from AutoEQ (7000+ models)"
                           FontSize="11"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Margin="0,0,0,12"
                           TextWrapping="Wrap" />

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0"
                                x:Name="HeadphoneSearchBox"
                                PlaceholderText="e.g. SteelSeries Arctis Nova Pro, HyperX Cloud, Sennheiser HD 600..."
                                Icon="{ui:SymbolIcon Search24}"
                                TextChanged="OnSearchTextChanged" />
                    <ui:Button Grid.Column="1"
                              x:Name="SearchBtn"
                              Content="Search"
                              Icon="{ui:SymbolIcon Search24}"
                              Appearance="Primary"
                              Click="OnSearchClick"
                              Margin="8,0,0,0" />
                </Grid>

                <!-- Search Results -->
                <ListBox x:Name="SearchResultsList"
                         MaxHeight="200"
                         Margin="0,0,0,12"
                         BorderThickness="1"
                         BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                         Visibility="Collapsed"
                         SelectionChanged="OnSearchResultSelected">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8" CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="12"
                                                   FontWeight="SemiBold" />
                                        <TextBlock FontSize="10"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                            <Run Text="{Binding Type}" />
                                            <Run Text=" — " />
                                            <Run Text="{Binding MeasurementSource}" />
                                        </TextBlock>
                                    </StackPanel>
                                    <ui:SymbolIcon Grid.Column="2"
                                                   Symbol="Download24"
                                                   FontSize="14"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Loading indicator -->
                <StackPanel x:Name="SearchLoading"
                            Orientation="Horizontal"
                            Visibility="Collapsed"
                            Margin="0,8,0,12">
                    <ProgressBar Width="16" Height="16" Margin="0,0,8,0"
                                 IsIndeterminate="True"
                                 Style="{StaticResource FluentProgressBarStyle}" />
                    <TextBlock Text="Searching..."
                               FontSize="11"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center" />
                </StackPanel>

                <!-- Downloaded Profile Preview -->
                <StackPanel x:Name="ProfilePreviewPanel" Visibility="Collapsed">
                    <TextBlock Text="Downloaded Profile"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8" />

                    <ui:CardControl Margin="0,0,0,8" Padding="12">
                        <ui:CardControl.Header>
                            <StackPanel>
                                <TextBlock FontSize="12"
                                           FontWeight="SemiBold">
                                    <Run Text="Headphone: " />
                                    <Run x:Name="ProfileHeadphoneName" />
                                </TextBlock>
                                <TextBlock FontSize="11"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                    <Run Text="Filters: " />
                                    <Run x:Name="ProfileFilterCount" />
                                    <Run Text=" • Preamp: " />
                                    <Run x:Name="ProfilePreamp" />
                                </TextBlock>
                            </StackPanel>
                        </ui:CardControl.Header>
                    </ui:CardControl>

                    <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="ProfileFiltersPreview"
                                   FontSize="10"
                                   FontFamily="Consolas"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                   Padding="12,8"
                                   Margin="0,0,0,12"
                                   TextWrapping="Wrap" />
                    </ScrollViewer>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button Content="Export to EqualizerAPO"
                                   Icon="{ui:SymbolIcon Save24}"
                                   Appearance="Primary"
                                   Click="OnExportProfile"
                                   Margin="0,0,8,0" />
                        <ui:Button Content="Use as Headphone Layer"
                                   Icon="{ui:SymbolIcon Checkmark24}"
                                   Appearance="Secondary"
                                   Click="OnUseAsBaseLayer" />
                    </StackPanel>
                </StackPanel>

                <!-- Python AutoEQ Generate -->
                <Separator Margin="0,16,0,16"
                           Background="{DynamicResource CardStrokeColorDefaultBrush}" />

                <TextBlock Text="Generate Custom EQ with AutoEQ"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4" />

                <TextBlock Text="Generate a fresh EQ profile from measurement data using the AutoEQ Python library"
                           FontSize="11"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Margin="0,0,0,8"
                           TextWrapping="Wrap" />

                <ui:CardControl x:Name="PythonStatusCard" Margin="0,0,0,8" Padding="12">
                    <ui:CardControl.Header>
                        <StackPanel>
                            <TextBlock FontSize="12" FontWeight="SemiBold">
                                <Run Text="Python: " />
                                <Run x:Name="PythonStatusText" Text="Checking..." />
                            </TextBlock>
                            <TextBlock x:Name="PythonErrorText"
                                       FontSize="10"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                    </ui:CardControl.Header>
                </ui:CardControl>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBox Grid.Column="0"
                                x:Name="PythonSearchBox"
                                PlaceholderText="Headphone name for Python generation (e.g. Sennheiser HD 650)"
                                Icon="{ui:SymbolIcon HeadphonesSoundWave24}"
                                TextChanged="OnPythonSearchChanged" />
                    <ui:Button Grid.Column="1"
                              x:Name="PythonGenerateBtn"
                              Content="Generate"
                              Icon="{ui:SymbolIcon ArrowDownload24}"
                              Appearance="Primary"
                              Click="OnPythonGenerate"
                              IsEnabled="False"
                              Margin="8,0,0,0" />
                </Grid>

                <ProgressBar x:Name="PythonProgress"
                             IsIndeterminate="True"
                             Visibility="Collapsed"
                             Margin="0,0,0,8"
                             Height="4" />

                <!-- Local Profiles -->
                <Separator Margin="0,16,0,16"
                           Background="{DynamicResource CardStrokeColorDefaultBrush}" />

                <TextBlock Text="Saved Headphone Profiles"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12" />

                <ItemsControl x:Name="LocalProfilesPanel">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:CardControl Margin="0,0,0,6" Padding="10">
                                <ui:CardControl.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="12"
                                                   VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <ui:Button Icon="{ui:SymbolIcon Delete24}"
                                                       Appearance="Danger"
                                                       Click="OnDeleteLocalProfile"
                                                       Tag="{Binding}"
                                                       Padding="4"
                                                       Margin="4,0,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </ui:CardControl.Header>
                            </ui:CardControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Bottom bar -->
        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Open AutoEQ Website"
                           Icon="{ui:SymbolIcon Globe24}"
                           Click="OnOpenAutoEq"
                           Margin="0,0,8,0" />
                <ui:Button Content="Close"
                           Appearance="Secondary"
                           Click="OnClose" />
            </StackPanel>
        </Border>

        <ui:SnackbarPresenter Grid.Row="1" x:Name="SnackbarPresenter" />
    </Grid>
</ui:FluentWindow>
